# `.lens` Version Table

## Current Version

| Surface | Current value | Notes |
| --- | --- | --- |
| `.lens` guidance version | `2026-05-19` | Single-repo policy, SSH deploy runbook, OTA preview schema, CI docs runbook |
| Public-doc promotion status | `draft` | Intended to move into broader docs later |

## Update Rules

Update this table when any of the following changes:

- the intended role of `.lens/`
- the server-vs-`.lens/` memory boundary
- the approved `.lens/` directory conventions
- the reference format for server-backed context
