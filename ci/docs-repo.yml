# Vendored docs CI (replaces inaccessible synthread/docs-viewer include).
# Stable channel: main. Preview channel: next/firmware-package, preview/*, MRs.

stages:
  - build
  - deploy

variables:
  HOME: "/tmp"
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"

.docs_rules_stable:
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
      variables:
        DOCS_CHANNEL: stable
    - if: $CI_COMMIT_TAG
      variables:
        DOCS_CHANNEL: stable

.docs_rules_preview:
  rules:
    - if: $CI_COMMIT_BRANCH == "next/firmware-package"
      variables:
        DOCS_CHANNEL: preview
    - if: $CI_COMMIT_BRANCH =~ /^preview\//
      variables:
        DOCS_CHANNEL: preview
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      variables:
        DOCS_CHANNEL: preview

build:docs:
  stage: build
  image: python:3.14-slim@sha256:c845af9399020c7e562969a13689e929074a10fd057acd1b1fad06a2fb068e97
  before_script:
    - export PATH="$HOME/.local/bin:$PATH"
    - pip install --quiet "mkdocs>=1.6,<2" "mkdocs-material>=9.5,<10" pymdown-extensions
  script:
    - mkdocs build
  artifacts:
    paths:
      - site/
    expire_in: 1 week
  rules:
    - !reference [.docs_rules_stable, rules]
    - !reference [.docs_rules_preview, rules]

pages:
  stage: deploy
  needs:
    - job: build:docs
      artifacts: true
  script:
    - mv site public
  artifacts:
    paths:
      - public
  rules:
    - !reference [.docs_rules_stable, rules]

build:n32probe:
  stage: build
  image: golang:1.26-bookworm@sha256:5f68ec6805843bd3981a951ffada82a26a0bd2631045c8f7dba483fa868f5ec5
  before_script:
    - apt-get update -qq && apt-get install -y -qq rsync
    - ./scripts/bootstrap-go-flash.sh
  script:
    - export SKIP_GO_FLASH_BOOTSTRAP=1
    - support/usb-validation/n32/build.sh
  artifacts:
    name: "n32probe-$CI_COMMIT_REF_SLUG-$CI_COMMIT_SHORT_SHA"
    paths:
      - build/n32probe_usb/
    expire_in: 2 weeks
  rules:
    - if: $CI_COMMIT_BRANCH == "next/firmware-package"
    - if: $CI_COMMIT_BRANCH =~ /^preview\//
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      changes:
        - support/usb-validation/**/*
        - scripts/bootstrap-go-flash.sh
        - deps/**/*
